## Oto

![oto](./images/oto.png)

Oto is a theme for the Zed code editor.

### Installation

```bash
git clone (this repo)
```

Then Run:

```bash
sudo find ~/oto/ -type f \( -iname '*.json' \) -exec cp {} ~/.config/zed/themes \;
```
